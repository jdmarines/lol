streamlit>=1.36.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
requests>=2.31.0
pyyaml>=6.0.1
# Si tu embedding usa PyTorch, descomenta la siguiente línea o instala según tu entorno:
# torch

# PyTorch (elige la variante CPU o CUDA según despliegue)
torch
